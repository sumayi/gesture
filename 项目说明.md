# 手势控制图片浏览

## 项目简介
基于 MediaPipe HandLandmarker 的实时手势识别应用，通过摄像头手势控制图片浏览（翻页和缩放），带中文语音播报。

## 项目结构

```
手势/
├── index.html              # Vite 入口 HTML
├── vite.config.js          # Vite 配置
├── package.json            # 依赖管理
├── .gitignore
├── 项目说明.md
├── docs/
│   ├── superpowers/
│   │   ├── specs/2026-05-16-gesture-image-control-design.md
│   │   └── plans/2026-05-16-gesture-image-control.md
├── public/
│   ├── wasm/               # MediaPipe 模型与 WASM 运行时
│   │   ├── hand_landmarker.task
│   │   ├── vision_wasm_internal.js
│   │   └── vision_wasm_internal.wasm
│   └── img/                # 浏览的图片
│       ├── 20260428-182932.jpg
│       ├── 20260428-182936.jpg
│       └── 20260428-182940.jpg
└── src/
    ├── main.jsx            # React 入口
    ├── index.css           # 全局样式
    ├── App.jsx             # 主组件（状态管理 + 手势路由）
    ├── hooks/
    │   └── useHandLandmarker.js  # HandLandmarker 初始化 + 检测循环 + 手势分类
    └── components/
        ├── ImageViewer.jsx      # 全屏图片显示 + 缩放/平移
        └── CameraOverlay.jsx    # 摄像头画中画小窗
```

## 技术栈

- **框架**: React 18 + Vite 5
- **手势识别**: MediaPipe Tasks Vision (`@mediapipe/tasks-vision`) — HandLandmarker
- **语音播报**: Web Speech API (`SpeechSynthesis`)，中文语音
- **摄像头**: `getUserMedia` 获取实时视频流
- **渲染**: Canvas 2D 绘制关键点叠加层

## 手势控制

| 手势 | 检测逻辑 | 操作 |
|------|---------|------|
| 比耶朝右 | 食+中指张开，无名+小指闭合，指尖朝右 | 下一张 |
| 比耶朝左 | 食+中指张开，无名+小指闭合，指尖朝左 | 上一张 |
| 张开手 | 四指全部张开 | 放大 +0.5x |
| 握拳 | 四指全部闭合 | 缩小 -0.5x |

## 使用方式

```bash
npm install    # 安装依赖
npm run dev    # 启动开发服务器
npm run build  # 构建生产版本
```

1. 打开 `http://localhost:5173`，允许摄像头权限
2. 做 **比耶** 手势（朝左/朝右）切换图片
3. 做 **张开手/握拳** 手势缩放当前图片
4. 摄像头小窗在右下角，支持鼠标拖拽平移放大后的图片
