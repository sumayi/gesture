# 手势控制图片浏览

## 项目简介
基于 MediaPipe HandLandmarker 的实时手势识别应用，通过摄像头手势控制图片浏览（翻页和连续捏合缩放），带画中画摄像头预览。

## 项目结构

```
手势/
├── index.html              # Vite 入口 HTML
├── vite.config.js          # Vite 配置
├── package.json            # 依赖管理
├── .gitignore
├── 项目说明.md
├── docs/superpowers/
│   ├── specs/2026-05-16-gesture-image-control-design.md
│   └── plans/2026-05-16-gesture-image-control.md
├── public/
│   ├── wasm/               # MediaPipe 模型与 WASM 运行时
│   └── img/                # 浏览的图片（3张）
└── src/
    ├── main.jsx
    ├── index.css
    ├── App.jsx
    ├── hooks/
    │   └── useHandLandmarker.js
    └── components/
        ├── ImageViewer.jsx      # 轮播图片 + 缩放/平移 + 缩略图
        └── CameraOverlay.jsx    # 画中画摄像头小窗
```

## 技术栈

- **框架**: React 18 + Vite 5
- **手势识别**: MediaPipe Tasks Vision — HandLandmarker
- **摄像头**: `getUserMedia`
- **渲染**: Canvas 2D

## 手势控制

| 手势 | 操作 |
|------|------|
| 比耶朝右 | 下一张（轮播动画） |
| 比耶朝左 | 上一张（轮播动画） |
| 捏合（拇指⊥食指距离） | 连续无级缩放 1x~5x |

比耶手势期间缩放锁定，不触发缩放变化。

## 界面特性
- 图片轮播滑动动画
- 底部缩略图图集（可点击跳转）
- 鼠标拖拽平移（缩放 > 1x 时）
- 右下角摄像头画中画

## 使用方式

```bash
npm install && npm run dev
```
